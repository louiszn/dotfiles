[project]
name = "louis-dotfiles-installer"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "pydantic>=2.0",
    "pyyaml>=6.0",
    "questionary>=2.0",
    "rich>=13.0",
]

[tool.uv]
package = false
