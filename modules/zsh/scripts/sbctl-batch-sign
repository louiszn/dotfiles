#!/bin/sh
set -euo pipefail

if [ "$(id -u)" -ne 0 ]; then
	echo "This script must be run as root." >&2
	exit 1
fi

sbctl verify | awk 'NR > 1 { print $2 }' | while IFS= read -r file; do
	[ -n "$file" ] && sbctl sign --quiet "$file"
done
